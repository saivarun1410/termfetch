{
  "name": "termfetch-launch-assistant",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node --check src/worker.js && node --check src/posts.js && node --check public/app.js && node --check public/sw.js",
    "test": "node --test tests/*.test.js",
    "build": "npm run check && npm test && wrangler deploy --dry-run --outdir dist",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "vapid:generate": "node scripts/generate-vapid.js",
    "access:generate": "node scripts/generate-access.js"
  },
  "dependencies": {
    "@block65/webcrypto-web-push": "1.0.2"
  },
  "devDependencies": {
    "web-push": "^3.6.7",
    "wrangler": "^4.30.0"
  }
}
